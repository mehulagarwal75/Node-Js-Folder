<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Dashboard</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #0f1117;
            min-height: 100vh;
            padding: 30px 20px;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            position: relative;
            overflow-x: hidden;
        }

        /* Animated background */
        .bg-overlay {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background: 
                radial-gradient(circle at 20% 30%, rgba(139, 92, 246, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(59, 130, 246, 0.08) 0%, transparent 50%);
            z-index: 0;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        /* Header Section */
        .dashboard-header {
            background: rgba(20, 22, 30, 0.8);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 40px 50px;
            margin-bottom: 30px;
            border: 1px solid rgba(139, 92, 246, 0.2);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .header-left h1 {
            font-size: 36px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .header-subtitle {
            color: #94a3b8;
            font-size: 15px;
        }

        .header-actions {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .btn-add {
            background: linear-gradient(135deg, #8b5cf6, #6366f1);
            color: white;
            padding: 14px 28px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
            border: none;
            box-shadow: 0 4px 16px rgba(139, 92, 246, 0.3);
        }

        .btn-add:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(139, 92, 246, 0.4);
        }

        .btn-add svg {
            width: 18px;
            height: 18px;
        }

        .stats-bar {
            display: flex;
            gap: 15px;
            background: rgba(139, 92, 246, 0.05);
            padding: 12px 20px;
            border-radius: 12px;
            border: 1px solid rgba(139, 92, 246, 0.15);
        }

        .stat-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: 600;
        }

        .stat-value {
            background: rgba(139, 92, 246, 0.2);
            color: #a78bfa;
            padding: 4px 10px;
            border-radius: 6px;
            font-weight: 700;
        }

        /* Table Card */
        .table-card {
            background: rgba(20, 22, 30, 0.8);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(139, 92, 246, 0.2);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
            overflow: hidden;
        }

        .table-container {
            overflow-x: auto;
        }

        .task-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        .task-table thead {
            background: rgba(139, 92, 246, 0.08);
        }

        .task-table thead th {
            padding: 20px 24px;
            text-align: left;
            font-size: 12px;
            font-weight: 700;
            color: #a78bfa;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
        }

        .task-table tbody tr {
            border-bottom: 1px solid rgba(139, 92, 246, 0.1);
            transition: all 0.3s ease;
        }

        .task-table tbody tr:hover {
            background: rgba(139, 92, 246, 0.05);
        }

        .task-table tbody tr:last-child {
            border-bottom: none;
        }

        .task-table tbody td {
            padding: 20px 24px;
            color: #e2e8f0;
            font-size: 14px;
            vertical-align: middle;
        }

        .task-id {
            color: #94a3b8;
            font-weight: 600;
            font-size: 13px;
            font-family: 'Monaco', monospace;
        }

        .task-name {
            font-weight: 600;
            color: #ffffff;
            max-width: 300px;
        }

        .task-date {
            color: #94a3b8;
            font-size: 13px;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .task-date svg {
            width: 14px;
            height: 14px;
            opacity: 0.6;
        }

        /* Badges */
        .badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }

        .badge-high {
            background: rgba(239, 68, 68, 0.15);
            color: #ef4444;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        .badge-medium {
            background: rgba(245, 158, 11, 0.15);
            color: #f59e0b;
            border: 1px solid rgba(245, 158, 11, 0.3);
        }

        .badge-low {
            background: rgba(34, 197, 94, 0.15);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .badge-completed {
            background: rgba(34, 197, 94, 0.15);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .badge-in-progress {
            background: rgba(59, 130, 246, 0.15);
            color: #3b82f6;
            border: 1px solid rgba(59, 130, 246, 0.3);
        }

        .badge-pending {
            background: rgba(245, 158, 11, 0.15);
            color: #f59e0b;
            border: 1px solid rgba(245, 158, 11, 0.3);
        }

        .badge-icon {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            display: inline-block;
        }

        .badge-high .badge-icon {
            background: #ef4444;
            box-shadow: 0 0 8px #ef4444;
        }

        .badge-medium .badge-icon {
            background: #f59e0b;
            box-shadow: 0 0 8px #f59e0b;
        }

        .badge-low .badge-icon {
            background: #22c55e;
            box-shadow: 0 0 8px #22c55e;
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 8px;
        }

        .btn-action {
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 12px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            transition: all 0.3s ease;
            border: 1px solid;
        }

        .btn-action svg {
            width: 14px;
            height: 14px;
        }

        .btn-edit {
            background: rgba(139, 92, 246, 0.1);
            color: #a78bfa;
            border-color: rgba(139, 92, 246, 0.3);
        }

        .btn-edit:hover {
            background: rgba(139, 92, 246, 0.2);
            border-color: rgba(139, 92, 246, 0.5);
            transform: translateY(-2px);
        }

        .btn-delete {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            border-color: rgba(239, 68, 68, 0.3);
        }

        .btn-delete:hover {
            background: rgba(239, 68, 68, 0.2);
            border-color: rgba(239, 68, 68, 0.5);
            transform: translateY(-2px);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 40px;
        }

        .empty-icon {
            width: 80px;
            height: 80px;
            background: rgba(139, 92, 246, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 24px;
        }

        .empty-icon svg {
            width: 40px;
            height: 40px;
            stroke: #8b5cf6;
        }

        .empty-title {
            font-size: 24px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 12px;
        }

        .empty-description {
            color: #94a3b8;
            font-size: 15px;
            margin-bottom: 24px;
        }

        @media (max-width: 768px) {
            body {
                padding: 20px 15px;
            }

            .dashboard-header {
                padding: 30px 25px;
                flex-direction: column;
                align-items: flex-start;
            }

            .header-left h1 {
                font-size: 28px;
            }

            .stats-bar {
                flex-direction: column;
                align-items: flex-start;
                width: 100%;
            }

            .task-table thead th,
            .task-table tbody td {
                padding: 16px 12px;
                font-size: 13px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn-action {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>

    <div class="bg-overlay"></div>

    <div class="container">

        <div class="dashboard-header">
            <div class="header-left">
                <h1>Task Dashboard</h1>
                <p class="header-subtitle">Manage and track all your tasks in one place</p>
            </div>
            <div class="header-actions">
                <div class="stats-bar">
                    <div class="stat-item">
                        Total: <span class="stat-value"><%= allDatatask.length %></span>
                    </div>
                    <div class="stat-item">
                        Pending: <span class="stat-value"><%= allDatatask.filter(t => t.status === 'pending').length %></span>
                    </div>
                    <div class="stat-item">
                        Completed: <span class="stat-value"><%= allDatatask.filter(t => t.status === 'completed').length %></span>
                    </div>
                </div>
                <a href="/addDataPage" class="btn-add">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="12" y1="5" x2="12" y2="19"></line>
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                    </svg>
                    Add New Task
                </a>
            </div>
        </div>

        <div class="table-card">
            <div class="table-container">
                <table class="task-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Task Name</th>
                            <th>Start Date</th>
                            <th>Due Date</th>
                            <th>Priority</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if(allDatatask.length === 0) { %>
                            <tr>
                                <td colspan="7" class="empty-state">
                                    <div class="empty-icon">
                                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <path d="M9 11l3 3L22 4"></path>
                                            <path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"></path>
                                        </svg>
                                    </div>
                                    <div class="empty-title">No Tasks Yet</div>
                                    <div class="empty-description">Get started by creating your first task</div>
                                    <a href="/addDataPage" class="btn-add" style="display: inline-flex;">
                                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <line x1="12" y1="5" x2="12" y2="19"></line>
                                            <line x1="5" y1="12" x2="19" y2="12"></line>
                                        </svg>
                                        Add New Task
                                    </a>
                                </td>
                            </tr>
                        <% } else { %>
                            <% allDatatask.forEach((task) => { %>
                                <tr>
                                    <td><span class="task-id">#<%= task.Id %></span></td>
                                    <td><span class="task-name"><%= task.Name %></span></td>
                                    <td>
                                        <span class="task-date">
                                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                                                <line x1="16" y1="2" x2="16" y2="6"></line>
                                                <line x1="8" y1="2" x2="8" y2="6"></line>
                                                <line x1="3" y1="10" x2="21" y2="10"></line>
                                            </svg>
                                            <%= task.AssignDate %>
                                        </span>
                                    </td>
                                    <td>
                                        <span class="task-date">
                                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                                                <line x1="16" y1="2" x2="16" y2="6"></line>
                                                <line x1="8" y1="2" x2="8" y2="6"></line>
                                                <line x1="3" y1="10" x2="21" y2="10"></line>
                                            </svg>
                                            <%= task.DueDate %>
                                        </span>
                                    </td>
                                    <td>
                                        <span class="badge badge-<%= task.priority.toLowerCase() %>">
                                            <span class="badge-icon"></span>
                                            <%= task.priority %>
                                        </span>
                                    </td>
                                    <td>
                                        <span class="badge badge-<%= task.status.toLowerCase().replace(' ', '-') %>">
                                            <%= task.status %>
                                        </span>
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <a href="/edittask?Id=<%= task.Id %>" class="btn-action btn-edit">
                                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                    <path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7"></path>
                                                    <path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                                                </svg>
                                                Edit
                                            </a>
                                            <a href="/removeTask?Id=<%= task.Id %>" class="btn-action btn-delete" onclick="return confirm('Are you sure you want to delete this task?')">
                                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                                    <polyline points="3 6 5 6 21 6"></polyline>
                                                    <path d="M19 6v14a2 2 0 01-2 2H7a2 2 0 01-2-2V6m3 0V4a2 2 0 012-2h4a2 2 0 012 2v2"></path>
                                                </svg>
                                                Delete
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

    </div>

</body>

</html>